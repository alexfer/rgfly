{% extends 'base.html.twig' %}

{% block title %}Hello BlogController!{% endblock %}

{% block body %}
    <div class="page-title-area blog" style="max-height: 200px;">
        <div class="d-table">
            <div class="d-table-cell">
                <div class="container">
                    <div class="page-title-content">
                        <p>{{ 'text.contact.desc'|trans|raw }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <section class="section-area default bg-white">
        <div class="container">
            <div class="row">
                {% for entry in entries %}
                    <div class="blog-block col-lg-4 col-md-6 col-sm-6 mb-4">
                        <div class="card shadow-sm h-100">
                            <div class="blog-img-block">
                                <img src="{{ asset('http://placehold.it/380x200') }}" class="blog-img"/>
                            </div>
                            <div class="card-body p-3">
                                <a href="{{ path('app_blog_view', {slug: entry.slug}) }}">
                                    {{ entry.entryDetails.title }}
                                </a>
                            </div>
                            <div class="card-footer text-right">
                                <ul>
                                    <li>
                                        <a target="_blank" href="{{ asset('https://www.facebook.com/AlexanderShtyher/') }}">
                                            @{{ entry.user.userDetails.firstName }}
                                        </a>
                                        <i class="bi bi-person-fill"></i>
                                    </li>
                                    <li>
                                        <i class="text-muted bi bi-calendar-date"></i> <small>{{ entry.createdAt|date(date_format) }}</small>
                                    </li>
                                    <li>
                                        <a href="">
                                            <i class="bi bi-facebook"></i>
                                        </a>
                                    </li>
                                    <li>
                                        <a href="">
                                            <i class="bi bi-twitter"></i>
                                        </a>
                                    </li>
                                    <li>
                                        <a href="">
                                            <i class="bi bi-instagram"></i>
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock %}
