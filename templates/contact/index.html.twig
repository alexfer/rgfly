{% extends 'base.html.twig' %}

{% block title %}{{ 'text.contact.header'|trans }}{% endblock %}

{% block body %}
    <div class="page-title-area contact">
        <div class="d-table">
            <div class="d-table-cell">
                <div class="container">
                    <div class="page-title-content">
                        <h2>{{ 'text.contact.header'|trans }}</h2>
                        <p>{{ 'text.contact.desc'|trans|raw }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <section class="section-area default">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5>Something else</h5>
                </div>
                <div class="col-md-8">
                    {{ include('contact/_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
