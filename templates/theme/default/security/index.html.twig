{% extends 'base.html.twig' %}

{% block title %}{{ 'text.login'|trans }}{% endblock %}

{% block body %}
    <div class="page-title-area default">
        <div class="d-table">
            <div class="d-table-cell">
                <div class="container">
                    <div class="page-title-content">
                        <h2>{{ 'text.login'|trans }}</h2>
                        <p>{{ 'text.login.desc'|trans|raw }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <section class="section-area default">
        <div class="container">
            <div class="row">
                <div class="col card bg-light-subtle">
                    {{ include('security/_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
