{% extends 'dashboard/orders.html.twig' %}

{% set _title_extend = 'title.dashboard.overview_news'|trans %}
{% block title_extend %}{{ _title_extend }} - {% endblock %}
{% block title_content %}{{ _title_extend }}{% endblock %}

{% block content %}
    <div class="table-responsive card">
        <table class="table table-hover table-borderless mb-0">
            <thead>
            <tr>
                <th class="bg-secondary text-white col text-center" scope="col">{{ 'table.header.id'|trans }}</th>
                <th class="bg-secondary text-white col" scope="col">{{ 'table.header.title'|trans }}</th>
                <th class="bg-secondary text-white col" scope="col">{{ 'table.header.author'|trans }}</th>
                <th class="bg-secondary text-white col text-center" scope="col">{{ 'table.header.created_at'|trans }}</th>
                <th class="bg-secondary text-white col text-center" scope="col">{{ 'table.header.handle'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for entry in entries %}
                <tr>
                    <th scope="row" class="text-center">{{ entry.id }}</th>
                    <td>{{ entry.entryDetails.title }}</td>
                    <td>{{ entry.user.userDetails.firstName }} {{ entry.user.userDetails.lastName }}</td>
                    <td class="text-center">{{ entry.createdAt|date(date_format) }}</td>
                    <td class="text-center"><i class="bi bi-person-circle"></i></td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="text-center text-bg-light">{{ 'table.footer.not_found'|trans }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
